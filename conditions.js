// let access = false;
let age = 15;

// access = (age >= 18) ? true : false;

// console.log(access)

if (age >= 18) {
	console.log("Welcome to my site.")
} else {
	console.log("You are under 18 and can not see my site!")
}